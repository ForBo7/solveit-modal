# core


<!-- WARNING: THIS FILE WAS AUTOGENERATED! DO NOT EDIT! -->

------------------------------------------------------------------------

### provision_sandbox

``` python

def provision_sandbox(
    gpu:str='T4', # GPU type string, e.g. 'T4', 'A100'
    timeout:int=600, # Max sandbox lifetime in seconds
)->tuple: # (sandbox, host, port)

```

*Create Modal app, image, sandbox, and expose TCP tunnel.*

------------------------------------------------------------------------

### setup_ssh

``` python

def setup_ssh(
    sb, # Modal Sandbox object
    host, # Tunnel hostname
    port, # Tunnel port
)->object: # Paramiko SSH connection

```

*Inject public key, start SSH service, and verify connectivity.*

------------------------------------------------------------------------

### link_remote_kernel

``` python

def link_remote_kernel(
    ssh, # Paramiko SSH connection
    host, # Tunnel hostname
    port, # Tunnel port
)->None:

```

*Launch remote IPython kernel on sandbox and connect ipyfernel to it.*

------------------------------------------------------------------------

### gpu_on

``` python

def gpu_on(
    gpu:str='T4', # GPU type string
    timeout:int=600, # Max sandbox lifetime in seconds
)->tuple: # (sandbox, ssh)

```

*Provision a GPU sandbox, wire up SSH, and hijack cells onto a remote
kernel.*

------------------------------------------------------------------------

### gpu_off

``` python

def gpu_off(
    sb, # sandbox object from gpu_on
)->None:

```

*Restore local kernel, disconnect remote, and teardown sandbox. Must be
called from a %%local cell.*
