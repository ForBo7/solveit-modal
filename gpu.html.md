# gpu


<!-- WARNING: THIS FILE WAS AUTOGENERATED! DO NOT EDIT! -->

------------------------------------------------------------------------

### GPUEntry

``` python

def GPUEntry(
    tag, price, vram
):

```

*Initialize self. See help(type(self)) for accurate signature.*

``` python
GPUEntry('T4', 0.59, 16)
```

    T4 — $0.59/hr, 16GB

------------------------------------------------------------------------

### GPU

``` python

def GPU(
    args:VAR_POSITIONAL, kwargs:VAR_KEYWORD
):

```

*Initialize self. See help(type(self)) for accurate signature.*

``` python
gpu = GPU(); gpu
```

    GPU variants (as of 2026-05-22, from https://modal.com/pricing):
      A10             $1.10/hr   24GB
      A100-40GB       $2.10/hr   40GB
      A100-80GB       $2.50/hr   80GB
      B200            $6.25/hr  180GB
      H100            $3.95/hr   80GB
      H200            $4.54/hr  141GB
      L4              $0.80/hr   24GB
      L40S            $1.95/hr   48GB
      RTX-PRO-6000    $3.03/hr   96GB
      T4              $0.59/hr   16GB

------------------------------------------------------------------------

### GPU.cheapest

``` python

def cheapest(
    
):

```

*Call self as a function.*

``` python
gpu.cheapest()
```

    T4 — $0.59/hr, 16GB

------------------------------------------------------------------------

### GPU.by_budget

``` python

def by_budget(
    max_phr:float, # GPUEntry list for GPUs costing ≤ max_phr per hour
):

```

*Call self as a function.*

``` python
gpu.by_budget(.8)
```

    [T4 — $0.59/hr, 16GB, L4 — $0.80/hr, 24GB]

------------------------------------------------------------------------

### GPU.price_of

``` python

def price_of(
    tag:str, # GPUEntry for a given GPU tag
):

```

*Call self as a function.*

``` python
gpu.price_of('T4')
```

    T4 — $0.59/hr, 16GB

------------------------------------------------------------------------

### GPU.tags

``` python

def tags(
    
)->list: # all available GPU tags

```

*Call self as a function.*

``` python
gpu.tags()
```

    ['T4',
     'L4',
     'A10',
     'L40S',
     'A100-40GB',
     'A100-80GB',
     'RTX-PRO-6000',
     'H100',
     'H200',
     'B200']

------------------------------------------------------------------------

### GPU.most_expensive

``` python

def most_expensive(
    
):

```

*Call self as a function.*

``` python
gpu.most_expensive()
```

    B200 — $6.25/hr, 180GB

------------------------------------------------------------------------

### GPU.by_vram

``` python

def by_vram(
    min_gb:int, # GPUEntry list for GPUs with at least min_gb VRAM
):

```

*Call self as a function.*

``` python
gpu.by_vram(24)
```

    [L4 — $0.80/hr, 24GB,
     A10 — $1.10/hr, 24GB,
     L40S — $1.95/hr, 48GB,
     A100-40GB — $2.10/hr, 40GB,
     A100-80GB — $2.50/hr, 80GB,
     RTX-PRO-6000 — $3.03/hr, 96GB,
     H100 — $3.95/hr, 80GB,
     H200 — $4.54/hr, 141GB,
     B200 — $6.25/hr, 180GB]
